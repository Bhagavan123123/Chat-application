specVersion: "v1beta"
serviceId: "Chat-application"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Chat-application-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
